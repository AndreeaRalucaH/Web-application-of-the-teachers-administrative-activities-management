const link = "http://localhost:8080/";

const adminiRoute = link + "admini";

const adminLoginUpdate = link + "admin";

const profesoriRoute = link + "profesori";

const activitatiRoute = link + "activitati";

const gestiuneRoute = link + "gestiune";

const profesorAnRoute = link + "profesor"; //ruta pentru an activitate

const atasamenteRoute = link + "atasament";

const notitaRoute = link + "notite";

const activitatiAtasamenteRoute = link + "activitate";

const activitateProfRoute = link + "activitat";

const notitaProfRoute = link + "notita";

const activitatiOrdonateDupaData = link + "activitateDupaData"

export { adminiRoute, profesoriRoute, activitatiRoute, gestiuneRoute, profesorAnRoute, atasamenteRoute, notitaRoute, activitateProfRoute, activitatiAtasamenteRoute, notitaProfRoute, activitatiOrdonateDupaData, adminLoginUpdate }